# Instructions


```bash
# Clone this repository
git clone https://github.com/miike-ai/claude-desktop-fedora.git
cd claude-desktop-fedora

# Build the package (Defaults to .deb and cleans build files)
./build.sh

chmod +x ./<APP IMAGE FILE>
./<APP IMAGE FILE>
```
